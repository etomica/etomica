#$ -S /bin/bash
# /usr/users/apoorvap/myfiles/newshiftedsimulatn-10power8steps/one
# /usr/users/apoorvap/myfiles/newshiftedsimulatn-10power8steps/one
#$ -cwd
 
echo $JOB_NAME $JOB_ID `hostname` `date` >> ~/q.log

java -cp etomica-apps_main.jar etomica.normalmode.SimLJHTTISuperVac -temperature $1 -density $2 -noDoVac > simT${1}rho${2}perf.out

echo $JOB_NAME $JOB_ID `hostname` `date` >> ~/q2.log
 

 