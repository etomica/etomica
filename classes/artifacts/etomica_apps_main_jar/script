#$ -S /bin/bash
# /usr/users/apoorvap/myfiles/densityfromlatticesite/250bins
# /usr/users/apoorvap/myfiles/densityfromlatticesite/250bins
#$ -cwd
 
echo $JOB_NAME $JOB_ID `hostname` `date` >> ~/q.log

java -cp etomica-apps_main.jar etomica.mappedDensity.mappedDensityfromlatticesite.MappedDensityfromlattice -temperature $1 -density $2 > simT${1}rho${2}.out

echo $JOB_NAME $JOB_ID `hostname` `date` >> ~/q2.log
 

    