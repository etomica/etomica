package etomica.virial.cluster2.ui.wizards;


public class ModelFactory {

  public Model createNext(Model current) {

    Model model = null;
    // select the next partial model based on the current (and valid) partial model
    return model;
  }
}
