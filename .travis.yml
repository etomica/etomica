notifications:
  email: false
dist: trusty
sudo: false
addons:
  apt:
    packages:
      - python-numpy
language: java
jdk:
  - oraclejdk8
before_script:
  - "pyenv install 3.4.0"
  - "pyenv global 3.4.0"
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - sleep 3 # give xvfb some time to start
